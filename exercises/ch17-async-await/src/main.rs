// Chapter 17: Async and Await
// Book: https://rust-book.cs.brown.edu/ch17-00-async-await.html
//
// Futures, async/await syntax, concurrency with async, streams.
//
// Workflow:
//   1. Read the chapter (and do the in-browser quizzes -- they aren't
//      trackable in git, so just get them to 100% before moving on).
//   2. Take notes in notes/ch17-async-await.md
//   3. Replace this stub with real code: re-type the chapter's examples
//      from memory (don't copy-paste), then extend or add your own
//      small exercise that uses the chapter's concepts.
//   4. cargo run -p ch17-async-await   /   cargo test -p ch17-async-await
//   5. git add -A && git commit -m "ch17: Async and Await"

fn main() {
    println!("Chapter 17 -- Async and Await: not started yet");
}

#[cfg(test)]
mod tests {
    #[test]
    fn placeholder() {
        // Replace with a real test once you've written real code.
        assert_eq!(1 + 1, 2);
    }
}
