// Chapter 14: More about Cargo and Crates.io
// Book: https://rust-book.cs.brown.edu/ch14-00-more-about-cargo.html
//
// Release profiles, publishing, workspaces (meta: you're already using one), cargo install.
//
// Workflow:
//   1. Read the chapter (and do the in-browser quizzes -- they aren't
//      trackable in git, so just get them to 100% before moving on).
//   2. Take notes in notes/ch14-more-about-cargo.md
//   3. Replace this stub with real code: re-type the chapter's examples
//      from memory (don't copy-paste), then extend or add your own
//      small exercise that uses the chapter's concepts.
//   4. cargo run -p ch14-more-about-cargo   /   cargo test -p ch14-more-about-cargo
//   5. git add -A && git commit -m "ch14: More about Cargo and Crates.io"

fn main() {
    println!("Chapter 14 -- More about Cargo and Crates.io: not started yet");
}

#[cfg(test)]
mod tests {
    #[test]
    fn placeholder() {
        // Replace with a real test once you've written real code.
        assert_eq!(1 + 1, 2);
    }
}
