// Chapter 10: Generic Types, Traits, and Lifetimes
// Book: https://rust-book.cs.brown.edu/ch10-00-generics.html
//
// The three hardest syntax topics after ownership. Traits especially are everywhere in idiomatic Rust.
//
// Workflow:
//   1. Read the chapter (and do the in-browser quizzes -- they aren't
//      trackable in git, so just get them to 100% before moving on).
//   2. Take notes in notes/ch10-generics-traits-lifetimes.md
//   3. Replace this stub with real code: re-type the chapter's examples
//      from memory (don't copy-paste), then extend or add your own
//      small exercise that uses the chapter's concepts.
//   4. cargo run -p ch10-generics-traits-lifetimes   /   cargo test -p ch10-generics-traits-lifetimes
//   5. git add -A && git commit -m "ch10: Generic Types, Traits, and Lifetimes"

fn main() {
    println!("Chapter 10 -- Generic Types, Traits, and Lifetimes: not started yet");
}

#[cfg(test)]
mod tests {
    #[test]
    fn placeholder() {
        // Replace with a real test once you've written real code.
        assert_eq!(1 + 1, 2);
    }
}
