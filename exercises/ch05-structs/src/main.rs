// Chapter 05: Using Structs to Structure Related Data
// Book: https://rust-book.cs.brown.edu/ch05-00-structs.html
//
// Defining structs, methods, associated functions.
//
// Workflow:
//   1. Read the chapter (and do the in-browser quizzes -- they aren't
//      trackable in git, so just get them to 100% before moving on).
//   2. Take notes in notes/ch05-structs.md
//   3. Replace this stub with real code: re-type the chapter's examples
//      from memory (don't copy-paste), then extend or add your own
//      small exercise that uses the chapter's concepts.
//   4. cargo run -p ch05-structs   /   cargo test -p ch05-structs
//   5. git add -A && git commit -m "ch05: Using Structs to Structure Related Data"

fn main() {
    println!("Chapter 05 -- Using Structs to Structure Related Data: not started yet");
}

#[cfg(test)]
mod tests {
    #[test]
    fn placeholder() {
        // Replace with a real test once you've written real code.
        assert_eq!(1 + 1, 2);
    }
}
