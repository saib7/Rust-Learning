// Chapter 15: Smart Pointers
// Book: https://rust-book.cs.brown.edu/ch15-00-smart-pointers.html
//
// Box, Deref, Drop, Rc, RefCell, reference cycles.
//
// Workflow:
//   1. Read the chapter (and do the in-browser quizzes -- they aren't
//      trackable in git, so just get them to 100% before moving on).
//   2. Take notes in notes/ch15-smart-pointers.md
//   3. Replace this stub with real code: re-type the chapter's examples
//      from memory (don't copy-paste), then extend or add your own
//      small exercise that uses the chapter's concepts.
//   4. cargo run -p ch15-smart-pointers   /   cargo test -p ch15-smart-pointers
//   5. git add -A && git commit -m "ch15: Smart Pointers"

fn main() {
    println!("Chapter 15 -- Smart Pointers: not started yet");
}

#[cfg(test)]
mod tests {
    #[test]
    fn placeholder() {
        // Replace with a real test once you've written real code.
        assert_eq!(1 + 1, 2);
    }
}
