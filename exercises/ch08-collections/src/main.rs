// Chapter 08: Common Collections
// Book: https://rust-book.cs.brown.edu/ch08-00-common-collections.html
//
// Vec, String, HashMap in depth. You will use these constantly.
//
// Workflow:
//   1. Read the chapter (and do the in-browser quizzes -- they aren't
//      trackable in git, so just get them to 100% before moving on).
//   2. Take notes in notes/ch08-collections.md
//   3. Replace this stub with real code: re-type the chapter's examples
//      from memory (don't copy-paste), then extend or add your own
//      small exercise that uses the chapter's concepts.
//   4. cargo run -p ch08-collections   /   cargo test -p ch08-collections
//   5. git add -A && git commit -m "ch08: Common Collections"

fn main() {
    println!("Chapter 08 -- Common Collections: not started yet");
}

#[cfg(test)]
mod tests {
    #[test]
    fn placeholder() {
        // Replace with a real test once you've written real code.
        assert_eq!(1 + 1, 2);
    }
}
