// Chapter 09: Error Handling
// Book: https://rust-book.cs.brown.edu/ch09-00-error-handling.html
//
// panic! vs Result, the ? operator, when to unwrap and when not to.
//
// Workflow:
//   1. Read the chapter (and do the in-browser quizzes -- they aren't
//      trackable in git, so just get them to 100% before moving on).
//   2. Take notes in notes/ch09-error-handling.md
//   3. Replace this stub with real code: re-type the chapter's examples
//      from memory (don't copy-paste), then extend or add your own
//      small exercise that uses the chapter's concepts.
//   4. cargo run -p ch09-error-handling   /   cargo test -p ch09-error-handling
//   5. git add -A && git commit -m "ch09: Error Handling"

fn main() {
    println!("Chapter 09 -- Error Handling: not started yet");
}

#[cfg(test)]
mod tests {
    #[test]
    fn placeholder() {
        // Replace with a real test once you've written real code.
        assert_eq!(1 + 1, 2);
    }
}
